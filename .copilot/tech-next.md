# Next.js Standard (App Router)

- **Components:** Default to Server Components. Use `'use client'` sparingly.
- **SEO:** Use dynamic metadata and JSON-LD for Salon pages.
- **Images:** Always use `next/image` for optimization.
- **Data Fetching:** Use `fetch` with appropriate revalidation tags.
- **Routing:** Use Parallel Routes for booking modals.
